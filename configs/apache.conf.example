<VirtualHost *:80>
        ServerName your.prefered.domain

        DocumentRoot /Your/Web/Directory
        <Directory /Your/Web/Directory>
                Options +Indexes +Includes +FollowSymLinks
                AllowOverride All
                Require all granted
        </Directory>

        #Only if you use php-fpm
        <FilesMatch \.php$>
                SetHandler "proxy:unix:/var/run/php/php7.0-fpm.sock|fcgi://localhost/"
        </FilesMatch>
        <IfModule setenvif_module>
                SetEnvIfNoCase ^Authorization$ "(.+)" HTTP_AUTHORIZATION=$1
        </IfModule>

        ErrorLog ${APACHE_LOG_DIR}/bcf-presentation_error.log
        CustomLog ${APACHE_LOG_DIR}/bcf-presentation_access.log combined
</VirtualHost>
